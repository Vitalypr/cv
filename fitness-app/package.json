{
  "name": "fitness-training-app",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first Hebrew RTL fitness/training tracker",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 5180 --bind 127.0.0.1",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "eval": "playwright test --grep @eval",
    "lint": "echo \"(lint configured in Phase 1)\" && exit 0",
    "typecheck": "echo \"(typecheck configured in Phase 1)\" && exit 0",
    "test": "echo \"(unit tests configured in Phase 1)\" && exit 0"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
